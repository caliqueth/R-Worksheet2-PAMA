---
title: "Untitled"
author: "Miko Pama"
date: "2023-11-08"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

#1
```{r}
matrixA <- matrix(0, nrow = 5, ncol = 5)

for (i in 1:5) {
  for (j in 1:5) {
    matrixA[i, j] <- abs(vectorA[i] - vectorA[j])
  }
}

matrixA
```

#2
```{r}
num_rows <- 5

for (i in 1:num_rows) {
  for (j in 1:i) {
    cat("* ")
  }
  cat("\n")
}
```

#3
```{r number3}
n <- as.numeric(readline("Enter the starting number for the Fibonacci sequence:"))


a <- n
b <- n


cat(a, " ")


repeat {
  c <- a + b
  if (c > 500) {
    break
  }
  cat(c, " ")
  a <- b
  b <- c
}
cat("\n")

```

#4
```{r number4a}
library(readr)
shoe <- read_csv("shoe.csv")
shoe
```

```{r number4b}
shoes <- read.csv("shoe.csv")
shoe
size <- shoe[c(1:6)]
size
```


```{r number4c}
household <- read.csv("HouseholdData.csv")
household
 
gender_counts <- table(household$Sex)

plot(1, type = "n", main = "Number of Males and Females in Household Data",
     xlab = "Sex", ylab = "Count", xlim = c(-0.1, 2.5), ylim = c(0, max(gender_counts)))

barplot(gender_counts, col = c("pink", "violet"), add = TRUE)

legend("topright", legend = levels(as.factor(household$Sex)), fill = c("pink", "violet"))
```

#5
```{r number5}
pie_chart <- c(60, 10, 5, 25)
pie(pie_chart, labels = paste0(pie_chart, "%"),
    main = "Monthly income of Dela Cruz family was spent on the following: ", col = rainbow(length(pie_chart)))

legend("topleft", legend = c("Food", "Electricity", "Savings", "Miscellaneous"),
       fill = rainbow(length(pie_chart)))
```